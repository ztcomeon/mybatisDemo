<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatisDemo.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.mybatisDemo.entity.User">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="userName" jdbcType="VARCHAR" property="userName"/>
        <result column="passWord" jdbcType="VARCHAR" property="passWord"/>
        <result column="realName" jdbcType="VARCHAR" property="realName"/>

        <!-- association属性必需在 collection之前写，不然会报错-->
        <!--<association property="cat" javaType="com.example.mybatisDemo.entity.Cat">-->
        <!--<id property="id" column="id"></id>-->
        <!--<result property="name" column="name"></result>-->
        <!--<result property="user" column="user_id"></result>-->
        <!--</association>-->

        <collection property="dog" ofType="com.example.mybatisDemo.entity.Dog">
            <id property="id" column="id"></id>
            <result property="name" column="name"></result>
        </collection>
    </resultMap>

    <!--<select id="findById" resultMap="BaseResultMap">-->
    <!--SELECT * FROM `user` u where u.id=#{id}-->
    <!--</select>-->

    <select id="findById" resultMap="BaseResultMap">
       SELECT * FROM `user` u left join dog d on u.id=d.user_id where u.id=#{id}
    </select>

    <insert id="createUser" parameterType="com.example.mybatisDemo.entity.User">
      insert into user(userName,passWord,realName) values (#{userName},#{passWord},#{realName} )
    </insert>

</mapper>